    'password' => [
        'required',
        'string',
        'min:8',
        'regex:/[a-zA-Z]/',    // Должна быть буква
        'regex:/[0-9]/',        // Должна быть цифра
        'regex:/[@$!%*?&]/',    // Должен быть специальный символ
    ],
